export type AppTheme = {
    colors: {
        primary: string,
        background: string,
        foreground: string,
        error: string,
        typography: string,
        input: string,
        button_hover: string
    },
    media: {
        sm: number
    }
}